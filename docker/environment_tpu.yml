name: imgx
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.9
  - pip=23.0.1
  - pip:
      - --find-links
        https://storage.googleapis.com/jax-releases/libtpu_releases.html
      - tensorflow-cpu==2.12.0
      - jax[tpu]==0.4.8
      - jaxlib==0.4.7
      - -r requirements.txt
